const isReactiveSymbol = Symbol.for('reactive layer');
const isInteractableSymbol = Symbol.for('interactable layer');
const isEmitterSymbol = Symbol.for('emitter3');

export default {
  isReactiveSymbol,
  isInteractableSymbol,
  isEmitterSymbol,
};
